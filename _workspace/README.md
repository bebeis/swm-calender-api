# Workspace Handoffs

This directory is reserved for optional Codex harness handoff artifacts.

Use it for large multi-phase work where preserving intermediate context helps review or recovery. Do not add handoff files for simple edits.
