swm:
  match:
    duplicate-analysis:
      provider: ${DUPLICATE_ANALYSIS_PROVIDER:keyword}
  clients:
    gemini:
      api-key: ${GEMINI_API_KEY:}
      base-url: ${GEMINI_BASE_URL:https://generativelanguage.googleapis.com}
      model: ${GEMINI_MODEL:gemini-2.5-flash}
